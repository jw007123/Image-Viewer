#pragma once

#include "Utility/Literals.h"

namespace GUI
{
	struct SizeConsts
	{
		static constexpr usize optionsPanelWidth  = 350;
		static constexpr usize optionsPanelHeight = 720;

		static constexpr usize viewportWidth  = 1080;
		static constexpr usize viewportHeight = 720;
	};
}
